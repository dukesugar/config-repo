server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dukesugar/config-repo.git
          search-paths: '{application}'




